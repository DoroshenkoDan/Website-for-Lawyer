# Public site URL (canonical links, sitemap, OG)
NEXT_PUBLIC_SITE_URL=https://rglawyer.com.ua

# WordPress REST API root (server-only — never exposed to the browser)
API_BASE_URL=https://cms.rglawyer.com.ua/wp-json/wp/v2
# Public WP GET needs no auth; leave empty
API_KEY=

# SMTP for the contact form (server-only)
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
CONTACT_TO=r.guseinova.lawyer@gmail.com
